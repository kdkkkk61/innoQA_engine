# 프로젝트 설계 원칙 (Claude Code 필독)

> **모든 개발·수정·문서 작업 시작 전 이 파일을 반드시 읽는다.**
> 해당 작업의 태그를 찾아 연결된 문서를 읽은 뒤 진행한다.

---

## 작업별 참조 문서

| 문서 | 태그 |
|------|------|
| `docs/test_scenario_standard.md` | [기본 경로 — 필독] |
| `docs/scenario_0_scan.md` | [기본 경로 — sc0 UIScanner 스캔·삭제/숨김 감지·스키마 제외정책] |
| `docs/scenario_N_*.md` (1~6) | [기본 경로] |
| `docs/architecture.md` | [공통 — 새 pages/ 파일 추가 시] [공통 — docs/ 파일 작성·수정 시] |
| `docs/scan-architecture.md` | [공통 — 새 pages/ 파일 추가 시] [공통 — 새 test_*.py 파일 추가 시] [공통 — UIScanner 통합 결정 시] |
| `docs/ui-interaction.md` | [공통 — 클릭/인코딩 문제 발생 시] |
| `docs/issue-screenshot-rules.md` | [공통 — fail/warn 검증 항목 작성 시] |
| `docs/issue-card-rules.md` | [공통 — fail/warn 검증 항목 작성 시] [공통 — 결함 카드 단위·merge 결정 시] |
| `docs/test-pipeline.md` | [공통 — 새 test_*.py 파일 추가 시] |
| `docs/yaml-guide.md` | [확장 경로 — YAML 스캔 적용 시] |
| `docs/scan-output-format.md` | [확장 경로 — modal_form 전용] |
| `docs/TROUBLESHOOTING.md` | [공통 — 이슈 수정 후 필기록] |
| `docs/karpathy-examples.md` | [확장 경로 — 코딩 원칙 적용 예시 필요 시] |

---

## 고정 운영 규칙

### 1. 이슈 수정 후 즉시 기록
코드 버그·오동작·설계 오류 수정 시 → `docs/TROUBLESHOOTING.md` 에 항목 추가.
기록 없이 넘어가는 것은 규칙 위반이다.

### 2. 테스트 실패 시 로그 먼저
추측으로 수정하기 전에 반드시 로그를 읽는다.
- `logs/` — app.py 실행 로그
- `reports/runs/*.log` — pytest 출력 전체 (가장 최근 타임스탬프 파일)

### 3. 문서 작성 시 추측 금지 (코드 인용 기반)
`docs/` 의 동작/매핑/우선순위/패턴 기술은 **코드 인용으로만**.
- 통합·일반화·예측 표현 금지 (예: "통합 4단 chain", "추후 단계 추가", "B-1-C 예정").
  대신 코드의 실제 분기를 그대로 기술 ("text 경로 1단, toggle 경로 3단").
- 미래 작업/단계 표현 금지. 사실 표현만 ("현재 미지원" / "현재 미적용").
- 예시(`scan_test` 등 generic) 그대로 md 사용 금지. 우리 컨벤션 (`[AUTO]_*`, 시나리오 약어 등)
  실제 코드 값으로 표기.
- 확장 자체는 OK — 코드 인용으로 뒷받침되면 사용자 의도보다 풍부해도 됨.
  단, 사실 기반 확장에 한정.

---

## 코드 수정 전 확인 절차

1. **영향 범위 파악**: 수정 대상을 import하거나 호출하는 파일 목록 먼저 확인
2. **연계 파일 맥락 확인**: 관련 파일 실제로 읽기 (YAML ↔ runner ↔ 테스트 연계 흐름 포함)
3. **맥락 파악 후 즉시 실행**: 설명 없이 Edit 도구로 바로 수정
4. **금지**: 연계 파일 확인 없이 수정 대상 파일만 보고 바로 수정 시작

---

## 데이터 안전 규칙

- `[AUTO]` 접두사 항목만 생성·삭제 가능. 미충족 시 Page 클래스에서 Exception 발생
- 날짜본(영속·연계) 접두사: `[AUTO_<MMDD>]`(예 `[AUTO_0629]`) — sc1~5 일반 cleanup 제외(`startswith("[AUTO]")`가 `[AUTO_`는 불일치 → 자동 보존). 'KEEP' 용어 폐기, 날짜 유무로만 구분(날짜 없음=휘발성 / 날짜 있음=영속). 고정 `[AUTO_KEEP]`은 다른 날 이전 본 잔존 시 이름/참조 충돌이라 날짜본으로 대체(nPouch 레거시 코드는 `[AUTO_KEEP]` 유지)
- **Lifecycle / 날짜본 명명 / sc3↔sc4 연계 / 사용 규칙 상세**: `docs/scenario_6_suite_setup.md` 참조
- 이 규칙은 어떤 경우에도 우회하지 않는다

---

## 절대 하지 말아야 할 것

**파이프라인** → `docs/test-pipeline.md` 참조
- conftest/html_reporter 파이프라인 우회 금지
- ScanResult 객체 없이 print()만으로 결과 출력 금지

**아키텍처** → `docs/architecture.md` 참조
- `base_page.py`에 특정 페이지 전용 로직 추가 금지
- 하나의 테스트 함수에서 여러 시나리오 검증 금지
- 하드코딩된 sleep() 사용 금지 (wait_for_selector 또는 expect 사용)

**진행 방식**
- 사용자가 "다음"이라고 하기 전에 다음 시나리오·다음 페이지 코드 작성 금지
- 실행 확인 없이 작성된 코드는 즉시 제거 대상
- 백업 없이 기존 파일 덮어쓰기 금지

---

## 코딩 행동 원칙 (Karpathy Guidelines)

> Andrej Karpathy의 LLM 코딩 함정 관찰 기반. 모든 개발 작업에 항상 적용.
> 사소한 작업(오타 수정 등)은 판단으로 가볍게 — 비사소 작업에서 비용 큰 실수 줄이는 게 목적.
> 상세 코드 예시: `docs/karpathy-examples.md`

### 1. Think Before Coding — 가정·혼란·tradeoff 표면화

구현 전:
- 가정을 명시한다. 불확실하면 추측 말고 묻는다.
- 해석이 여러 개라면 묻지 않고 하나만 골라 진행하지 않는다 — 후보를 제시한다.
- 더 단순한 접근이 있으면 말한다. 필요하면 push back.
- 불명확하면 멈춘다. 무엇이 헷갈리는지 명시하고 묻는다.

### 2. Simplicity First — 문제를 푸는 최소 코드만

- 요청 외 기능 추가 금지.
- 일회성 코드에 추상화 금지.
- 요청하지 않은 "유연성", "설정 가능성" 금지.
- 일어날 수 없는 시나리오의 에러 처리 금지.
- 200줄 짠 게 50줄로 가능했으면 다시 쓴다.

자문: "시니어 엔지니어가 보면 과한가?" → Yes면 단순화.

### 3. Surgical Changes — 요청한 부분만, 본인이 만든 잔해만 정리

기존 코드 수정 시:
- 인접 코드·주석·포맷 "개선" 금지.
- 망가지지 않은 걸 리팩터하지 않는다.
- 본인 스타일과 달라도 기존 스타일에 맞춘다.
- 무관한 dead code를 발견하면 알리되 삭제하지 않는다.

본인 변경이 만든 고아 처리:
- 본인 변경으로 안 쓰이게 된 import/변수/함수만 제거.
- 기존부터 있던 dead code는 요청 없으면 제거 금지.

테스트: 변경된 모든 줄이 사용자 요청과 직접 연결되는가.

### 4. Goal-Driven Execution — 검증 가능한 성공 기준 정의 후 루프

작업을 검증 가능한 목표로 변환:
- "검증 추가" → "잘못된 입력 테스트 작성 후 통과시키기"
- "버그 고치기" → "버그 재현 테스트 작성 후 통과시키기"
- "X 리팩터" → "리팩터 전후 테스트 통과 보장"

다단계 작업은 짧은 계획 명시:
```
1. [단계] → 검증: [확인 기준]
2. [단계] → 검증: [확인 기준]
3. [단계] → 검증: [확인 기준]
```

강한 성공 기준 = 독립 루프 가능. 약한 기준("동작하게") = 매번 재확인 필요.

### 효과 지표

이 원칙들이 잘 적용되고 있다면:
- diff에 불필요한 변경이 줄어든다 (요청한 변경만).
- 과도복잡으로 인한 재작성이 줄어든다.
- 명확화 질문이 구현 전에 나온다 (실수 후 아닌).
- PR이 깔끔하고 작다 (드라이브-바이 리팩터·"개선" 없음).

---

## 활용 스킬 (시스템 로드, 적절한 시점에 자동 호출)

### `simplify`
**시점**: 코드 변경 후 / PR 전 / Karpathy 2번 원칙 강제 적용 시
**효과**: 변경된 코드를 재사용성·품질·효율 관점에서 검토하고 발견된 이슈 수정
**언제 자동**: "리뷰", "정리", "simplify", "단순화" 키워드 또는 코드 작업 마무리 시점

### `loop`
**시점**: 반복 작업 자동화 (빌드 모니터링, 회귀 테스트 폴링 등)
**효과**: 지정 간격(예: 5분)으로 명령 반복 실행. 모델 자체 페이싱도 가능
**언제 자동**: "5분마다 X 확인", "계속 모니터링", "주기적 실행" 요청 시

### `fewer-permission-prompts`
**시점**: 권한 prompt 자주 떠서 흐름 끊길 때
**효과**: 최근 transcripts 스캔 → 자주 쓰는 read-only 명령 자동으로 .claude/settings.json allowlist 추가
**언제**: 환경 구축 시 1회 / 권한 항목 어수선해질 때 정리

### `anthropic-skills:skill-creator`
**시점**: 프로젝트 특화 반복 패턴 발견 시 (예: 시나리오 4-3 검증 패턴이 정형화되면)
**효과**: 프로젝트 로컬 스킬 생성 (`.claude/skills/`)
**언제 자동**: "스킬 만들자", "skill create" 요청 시

### 그 외 자동 트리거 스킬 (이미 적용 중)

| 스킬 | 트리거 |
|------|-------|
| `bkit:pdca` | "plan", "design", "do", "analyze", "report" 등 PDCA 키워드 |
| `bkit:bkit-rules` | 모든 개발 작업 (자동) |
| `anthropic-skills:pptx` | `.pptx` 파일 작업 시 |
| `anthropic-skills:docx/xlsx/pdf` | 해당 형식 작업 시 |
