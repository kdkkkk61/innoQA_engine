# 제어 스위트 — 입력 필드 × 검증 유형 매트릭스

> 목적: 각 입력 필드가 [빈값 / 중복 / 형식오류 / 오버플로 / 경계] 검증을 모두 갖는지 점검.
> 빈칸(❓) = 누락 후보. 단 "검증 없음"이 제품 사실일 수 있으므로 Chrome MCP 확인 후 보완.
> 기준: yaml 검증 규칙 부재(scan_hint 3줄) → 제품 UI 동작이 기준.
> 작성: 2026-05-28 (사용자 통찰 — "필드마다 중복/오버/형식 검증 다 있어야 하는데 일부만 함")

## 검증 유형 정의

| 유형 | 의미 |
|------|------|
| 빈값 | 필수 필드 비우고 확인 → 차단 메시지 |
| 중복 | 동일 값 재추가 → "이미 등록" 차단 |
| 형식오류 | 잘못된 형식 (IP/Port 숫자, 특수문자) → 차단 |
| 오버플로 | 길이 초과 → DOM 절단 또는 server error |
| 경계(정상) | 허용 최대값 → 정상 저장 (대조군) |

## 매트릭스 (현재 커버 / ❓ 누락 후보)

| 입력 필드 | 빈값 | 중복 | 형식오류 | 오버플로 | 경계(정상) |
|----------|:----:|:----:|:--------:|:--------:|:----------:|
| 메인 csuName | 3e/4g | 3e/4g | N/A | 3h/4p(50절단) | — |
| 메인 clipboardUrl | — | N/A(textarea) | N/A(자유텍스트) | 3h/4p(길이) | — |
| 메인 customOption | — | — | — | 3h/4p | — |
| 메인 차단확장자 | — | 4k | ✅C(특수문자/한글 거부 alert) | ✅C 검증없음(길이無) | — |
| 메인 전자서명 | — | 3f/4k | — | ✅C 검증없음(300자 추가·저장안전 yaml:399) | — |
| 프로세스 IP/Port | ✅C(IP빈값 차단·무메시지) | 3f/4m | 3g(Port abc/IP)+✅C(IP999 차단) | 3g(99999)/3j(-1/빈값) | 3j(8080/0) |
| 프로세스 확장자 | — | 3f/4m | ✅C(특수문자 거부)·형식메시지 테스트 누락 | 길이無·저장OK(yaml:364 safe) | — |
| 프로세스 drive letter | — | — | ✅C 검증없음(가비지 허용) | 3j(100자) | 3j(50자) |
| 프로세스 cache | — | 3i-D/4n | — | 3j(500자) | — |
| 프로세스 설명 | — | — | — | 3g(1000→300) | — |
| 태그 (프로세스 동일 깊이) | — | 4e | 3g | 3j(drv/Port/cache) | — |
| 웹 이름 | 3f/4l | ✅C 차단(검증O·테스트 누락 🔴) | — | 3j(500) | 3j(100) |
| 웹 URL | — | 3f/4l + 일관성(3/4l) | 없음(제품사실) | 3j(1000) | — |
| 웹 확장자 | — | 3f/4l | ✅C(특수문자/한글 거부·trusted확인)+테스트 추가완료 | 3j J(500→서버오류) | — |
| 웹 basePath | — | — | — | 3j(400) | 3j(300) |
| 웹 업로드용량 | yaml:빈값→0 | — | yaml 마스킹(abc→0/-1→1/1.5→0) | yaml:≤12자 저장OK·13자 모달차단 | — |
| 웹 설명 | — | — | — | 3g(1000→300) | — |

## 빈칸 (누락 후보) — 우선순위

| 우선 | 필드 × 유형 | Chrome 확인 결과 (2026-05-28) |
|------|------------|---------------------|
| ✅ | 웹 업로드용량 — 빈값/형식/음수/초과 | yaml 기 검증 (abc→0/-1→1/1.5→0, 13자 모달차단). JS-probe 소수점=아티팩트 |
| 🔴 | 프로세스/웹 확장자 — 형식 메시지 | 특수문자 거부(검증 O). **거부 메시지 assert 테스트 누락 → 추가** |
| ✅ | drive letter — 형식 | **형식검증 없음** (가비지 그대로 허용) |
| ✅ | 메인 clipboardUrl — 중복/형식 | N/A — TEXTAREA(add버튼/list 없음), 중복·형식 개념 없음 |
| 🔴 | 메인 차단확장자 — 형식 메시지 | 형식 거부(검증 O), 길이 무검증. 거부 메시지 테스트 누락 → 추가 |
| ✅ | IP 빈값 | 차단됨(무메시지 silent) |
| ✅ | 전자서명 오버플로 | 검증 없음 (300자 추가됨, 저장 안전 — yaml:399) |
| ✅ | 웹 이름 중복 | 차단됨(검증 O) → sc3f Case7 + sc4l E **테스트 추가 완료** |

## 진행 절차 (사용자 결정 2026-05-28)

1. ✅ commit (40 passed 안정 상태) — `2d6a37e`
2. ✅ 매트릭스 md 작성 (이 문서)
3. ✅ 🔴 우선 빈칸 Chrome MCP 확인 (2026-05-28) → 아래 "Chrome MCP 확인 결과" 섹션
4. ✅ 실제 갭 테스트 추가 (2026-05-28) — 확장자 형식(특수문자) **거부 메시지 assert** (메인/프로세스/웹, sc3+sc4 대칭 6건)
   sc3: 메인/프로세스/웹 각 1건 / sc4: 4k A2(메인) / 4m B2(프로세스) / 4l C2(웹)
   (오버플로·업로드용량 = yaml 기 검증 또는 sc3j J 기 테스트 → 신규 불필요)
5. ✅ yaml 보강 (2026-05-28) — control_suite.yaml scenario_3_input_format 3항목에 test_coverage 주석 추가
   + 웹 확장자 한글 허용 chrome_mcp_recheck_needed 기록

## Chrome MCP 확인 결과 (2026-05-28) — ✅C 표기 근거

> 영역별 직접 입력 검증 (192.168.13.141 manager). 메시지는 window.alert 또는 #__globalMessageModal 로 노출.

### 메인 모달
| 필드 | 입력 | 결과 |
|------|------|------|
| 차단확장자 형식 | 특수문자/한글 | 거부 (alert) → **검증 있음** |
| 차단확장자 오버 | 장문 | maxlength 없음, 추가됨 → 입력단 길이검증 없음 |
| clipboardUrl/customOption/전자서명 | 임의 | free text, 형식검증 없음 |

### 프로세스 모달 (#controlSuiteProcessList)
| 필드 | 입력 | 결과 |
|------|------|------|
| 확장자 형식 | `a*b` 등 특수문자 | 거부 + #__globalMessageModal (`. * ; ?` 금지) → **검증 있음** (yaml:646 일치) |
| 확장자 오버 | 300자 ascii | maxlength 없음, 추가됨. 단 yaml:364 = **저장 OK(safe)** — 결함 아님 |
| IP 빈값 | IP="" + Port=8080 | 추가 차단 (메시지 없이 silent) |
| IP 형식 | `999.1.1.1` | 추가 차단 (silent) (yaml:337 format_error 일치) |
| Port 빈값 | IP=10.0.0.1 + Port="" | IP 추가됨 → 저장 시 서버오류 (sc3j G 일치, 이미 테스트됨) |
| drive letter 형식 | `12!@#가ABC` | 그대로 허용 → **형식검증 없음** (isAccessDrive ON 후) |

### 웹제한 모달 (#controlSuiteWebRestricList)
| 필드 | 입력 | 결과 |
|------|------|------|
| 확장자 형식 | `a*b` | 거부 + 메시지 → **검증 있음** (yaml:662 일치) |
| 확장자 형식 | 한글 (trusted) | **거부됨** (form_input 신뢰입력: after=0, `. * ; ?` 메시지) — 앞 JS-probe "추가"는 아티팩트 확정 (yaml:666 일치) |
| 확장자 오버 | 500자 | sc3j J / 4j J 에서 **이미 테스트됨** (메인 저장 서버오류) |
| 업로드용량 마스킹 | abc/-1/1.5 | **JS-probe 결과(1.5 유지)는 아티팩트** — yaml(실입력): abc→0, -1→1, 1.5→0 |
| 업로드용량 한도 | 13자 | yaml:354 = 모달 단계 차단("최대 12자"). 12자(999999999999)=저장 OK |

> ⚠ **JS-probe 한계**: uploadLimitSize 등 키보드 마스킹 필드는 `el.value=` 프로그램 주입이
> AngularJS keypress 마스크를 우회한다. masking 값 판정은 yaml 의 실입력(Chrome MCP trusted) 검증을 신뢰.

### 재확인으로 좁혀진 실제 갭 (yaml 대조 후)
- 🔴 **확장자 형식(특수문자) 차단 메시지 테스트 누락** — yaml 은 3영역(메인/프로세스/웹) 명세하나
  sc3/sc4 에 거부 메시지 assert 테스트 없음 (중복·추가/삭제만 존재) → **추가 대상**
- 🟢 웹 확장자 한글 허용 여부 — JS-probe 상충 → Chrome MCP trusted click 재확인 필요
- 🟢 IP 빈값/형식 차단이 무메시지(silent) — UX 기록 (검증 자체는 존재)
- ✅ 프로세스 확장자 오버 = 저장 OK(yaml:364) — 결함 아님 (테스트 불필요)
- ✅ 웹 확장자 오버 = sc3j J/4j J 기 테스트 / 업로드용량 = yaml 기 검증 (신규 갭 아님)

## 2차 Chrome 확인 — 잔여 미확인 셀 4건 (2026-05-28)

> 사용자 지적: "추정으로 메워둔 칸이 남아있다" → ❓ 셀 직접 확인.

| 셀 | 확인 방법 | 결과 |
|----|----------|------|
| 메인 clipboardUrl 중복/형식 | DOM 구조 | **N/A** — `#clipboardAllowUrl` = TEXTAREA, add버튼/list 없음 → 중복·형식 개념 자체 없음(자유텍스트) |
| 메인 전자서명 오버플로 | 300자 추가 | maxlength 없음, 추가됨, 메시지 없음 → **검증 없음** (yaml:399 저장 안전영역 일치) |
| 웹 확장자 한글 | form_input 신뢰입력 | **거부됨** (after=0, `. * ; ?` 메시지) → 앞 JS-probe "허용"은 아티팩트 확정 |
| 웹제한 이름 중복 | 동일 이름 2건 추가 | **차단됨** (메시지 노출·list 미추가·모달 유지) → **검증 있음, 테스트 누락** 🔴 |

### 2차 확인으로 새로 드러난 갭
- 🔴 **웹제한 이름 중복 차단 메시지 테스트 누락** — coverage-map #39 는 이름 *빈값*만(3f Case1/4l A), *중복* 없음.
  → "딱 하나(형식메시지)뿐"이 아니었음. 웹 이름 중복도 신규 테스트 대상 → 추가.
- 나머지 3건은 N/A 이거나 yaml 기검증 → 테스트 불필요 (사실 기록만).

## 참고 — "검증 없음"이 제품 사실인 케이스

- 웹 URL 형식 검증: 없음 (sc2e line 746-752 — 'not a url' 도 등록됨, yaml 사실)
- → 빈칸이 모두 누락은 아님. Chrome 확인으로 "제품이 검증하는가" 판별 필수.
